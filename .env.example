# Shared password for the site gate. Visitors enter it once at /login and
# get a year-long cookie. Unset in development means no gate; unset in
# production means every route returns 503.
SITE_PASSWORD=
